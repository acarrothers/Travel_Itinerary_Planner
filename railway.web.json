{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --no-frozen-lockfile --prod=false && pnpm --filter @trip-itinerary/web build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @trip-itinerary/web start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
